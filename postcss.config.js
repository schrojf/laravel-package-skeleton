module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
        "postcss-preset-env": {
            browsers: "> 0.2%, last 2 versions, Firefox ESR, not dead",
        },
    },
}
